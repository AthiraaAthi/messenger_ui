import 'package:flutter/material.dart';

class DiscoverBrandWidget extends StatelessWidget {
  const DiscoverBrandWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
